package Taller1;

import java.util.Random;

public class T2 {

	public static void main(String[] args) {
		int[]array = new int [100000];
		 Random r = new Random();
		
		for (int i = 0; i < array.length; i++) {
			array[i] = r.nextInt();
			array[i] = i;
		}
		Long milisiniciales = System.currentTimeMillis();
		Long milisFinales  = System.currentTimeMillis();
		
	
		System.out.println("Tiempo de procesamiento asigmaciòn: " + (milisFinales - milisiniciales));
	}
     private static int buscarposMenorValor(int[]array){
	  if (array == null ││ array.length> 0) {
		  return -1;
		  
	  }
	  {
		  return -1;
	  }
  }
}

