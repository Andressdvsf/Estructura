package nodo;

public class test {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Nodo arbol = new Nodo(10);
		arbol.insertar(new Nodo(5))
		arbol.insertar(new Nodo (20));
		arbol.insertar(new Nodo(1));
		System.out.println("Altura: " + arbol.calcularAltura);
		System.out.println("Arbol: ");
		System.out.println(arbol.toString);
		

	}

}
public String toString() {
	StringBuilder sb = new StringBuilder();
	

	if (izq != null && der != null) {
	  return " " + valor;
	
			  
		  }
	if(izq!= null) {
		
		sb.append(izq.toString());
	}
	sb.append(" ");
	if(der!= null){
		sb.append(der.toString());
		
	}
	sb.insert(0, null)
		
		{
			
		
			
		}else {
			int numeroLineas = altura;
			while (numeroLineas > 0) {
				
					
				}
				
			}
			
			int
		
		sb.append(System.lineSeparator());
		if(izq !=  null) {
			sb.append(izq.toString());
			
		}
		
		
		
	}else {
		for(int i= 0 ; i) 
	}
		System.out.print(valor);
	
}
